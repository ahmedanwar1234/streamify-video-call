module.exports = {
  extends: [
    "stylelint-config-standard",
    "stylelint-config-tailwindcss"  // adds all @tailwind, @apply, @screen, etc.
  ],
  rules: {
    // your overrides here...
  }
};

